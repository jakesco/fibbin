{
  "name": "fibbin",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "tsc && vite build && wrangler pages dev --local dist --kv=STORE --r2=BUCKET"
  },
  "dependencies": {
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.17.0",
    "@types/uuid": "^8.3.4",
    "typescript": "^4.8.4",
    "wrangler": "^2.1.11",
    "vite": "^3.1.0"
  }
}